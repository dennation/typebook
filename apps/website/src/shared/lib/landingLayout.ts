/** Shared layout class strings for the marketing landing sections. */

/** Centered max-width content column with responsive horizontal padding. */
export const CONTAINER = "max-w-295 mx-auto px-8 w-full max-[620px]:px-5";

/** Vertical section padding (collapses on small screens). */
export const SECTION_PAD = "py-30 max-[860px]:py-21";
