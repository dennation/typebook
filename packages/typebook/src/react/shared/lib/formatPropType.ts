// Moved to the React-free scanner core so `@dennation/typebook/scanner` can format types
// without pulling in the React layer. Re-exported here to keep existing import paths working.
export { formatPropType } from "../../../scanner/formatPropType";
