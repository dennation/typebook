---
"@dennation/typebook": minor
---

Add the opt-in `snippets()` transform plugin and a dependency-free `@dennation/typebook/react` `<Snippet>` runtime: the bundler slices each `<Snippet>` example's source and injects it as `__snippetSource`, which the runtime renders with a show-source toggle.
